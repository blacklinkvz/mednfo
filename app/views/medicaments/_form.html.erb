<%= form_with(model: medicament) do |form| %>
  <% if medicament.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medicament.errors.count, "error") %> prohibited this medicament from being saved:</h2>

      <ul>
        <% medicament.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :compound, style: "display: block" %>
    <%= form.text_field :compound %>
  </div>

  <div>
    <%= form.label :unit_id, style: "display: block" %>
    <%= form.text_field :unit_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
