<%= form_with(model: articles_disease) do |form| %>
  <% if articles_disease.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(articles_disease.errors.count, "error") %> prohibited this articles_disease from being saved:</h2>

      <ul>
        <% articles_disease.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Articulo, style: "display: block" %>
    <%= form.collection_select :article_id, @articles.where(user_id: current_user.id), :id, :title, prompt: "Selecciona el viaje", class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div>
    <%= form.label :Enfermedad, style: "display: block" %>
    <%= form.collection_select :disease_id, @diseases, :id, :name, prompt: "Selecciona una enfermedad", class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
